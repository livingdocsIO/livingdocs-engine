describe "DOM method", ->

  # # parentSnippet()
  # it 'parentSnippet() should find H1 snippet from H1 node', ->
  #   snippet = test.getSnippet('title')
  #   node = snippet.$snippet[0]
  #   expect( dom.parentSnippet(node) ).toEqual(snippet)

  # it 'parentSnippet() should find row snippet from child node ', ->
  #   snippet = test.getSnippet('row')
  #   node = snippet.$snippet.find('.span8').first()[0]
  #   expect( dom.parentSnippet(node) ).toEqual(snippet)
